'use strict'

const addtext = document.getElementById('control-add') // кнопка add
// add.addEventListener('click', function(){
    
// })
const deleteAll = document.getElementById('control-delete')  // кнопка delete
const enterText = document.getElementById('control-enter')  // поле с текстом
const completeTodo = document.getElementById('todo-complete')  // кнопка галочка, которая убирает лист с зеленым цветом экрана
const closeTodo = document.getElementById('todo-close')  // кнопка крестик, которая убирает лист с красным цветом экрана
